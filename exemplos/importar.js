const add = (a,b) => a + b
const subtrai = (a,b)  =>  a - b

const link = (busca, local)  =>  'https://www.google.com/maps/search/' + busca + "+" + local




module.exports = { add, subtrai, link}